# Smart Recruit

Update route to be consistent
Remove scope on all
    register
    auth
When editing with joins, need to set values

feedback categories/type
UserSkills
UserWallet
tbl_project_dispute